// there will be two types, local and interlocutors'
// is this where active callouts are stored?

// perhaps set up handler here then edit Calloutable.js in activate and deactivate 

// sync it with brahma but also make sure local calloutable is unchanged

// function CalloutActiveHandler() {
//   this.callouts = {};
// }

// CalloutActiveHandler.prototype.addCallout = function(callout) {
//   this.callouts[callout.id] = callout;
// };

// CalloutActiveHandler.prototype.removeCallout = function(callout) {
//   delete this.callouts[callout.id];
// };

// CalloutActiveHandler.prototype.getCallout = function(id) {
//   return this.callouts[id];
// };


